describe('how to write a spec', function () {
    it("is easy if you know how", function () {
        const a = 10, b = 20;

        const answer = a + b;

        expect(answer).toBe(30);
    })
})

describe('trying out the snippets', () => {
    it('is fun!', () => {

    });
});